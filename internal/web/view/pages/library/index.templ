package library

import "github.com/ListenUpApp/ListenUp/internal/web/view/layouts"

templ LibraryIndexPage(pageName string) {
    @layouts.App(pageName) {
        @LibraryIndexContent()
    }
}

templ LibraryIndexContent() {
    <div class="prose lg:prose-xl p-4">
        <h1 class="text-2xl">Library Page</h1>
        <p>If You had books they would be here.</p>
    </div>
}