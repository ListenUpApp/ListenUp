package com.calypsan.listenup.client.features.shell.components

import androidx.compose.ui.unit.dp

/**
 * Height of the shell bottom navigation bar content — used to position elements
 * (e.g. the now-playing host) above it. Does not include system gesture insets.
 */
val NavigationBarHeight = 80.dp
