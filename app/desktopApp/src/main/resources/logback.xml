<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--
        File persistence goes through the shared FileLogSink (size-rotated, ~2 MB cap,
        written to the app data dir's logs/ folder — the same files the Settings
        "Share logs" action reveals). This appender only forwards; see ListenUpFileAppender.
    -->
    <appender name="FILE" class="com.calypsan.listenup.desktop.logging.ListenUpFileAppender"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Quieter logging for noisy libraries -->
    <logger name="io.ktor" level="WARN"/>
    <logger name="org.koin" level="INFO"/>
    <logger name="org.jetbrains.skia" level="WARN"/>
    <logger name="androidx.compose" level="WARN"/>
</configuration>
