# AGENTS.md

ListenUp's AI agent instructions live in **[`CLAUDE.md`](CLAUDE.md)** at the workspace root — read that file first.

This file exists only so agent tooling that specifically looks for `AGENTS.md` (rather than `CLAUDE.md`) finds the same guidance. Don't let the two drift: if you're updating one, update both, or better, replace this file's content with a pointer like this one so there's only one copy of the actual rules to maintain.

For the day-to-day engineering rules (module layout, error architecture, testing, commit/push conventions), the canonical file is **`ListenUp/CLAUDE.md`** inside the repository — `CLAUDE.md` at this workspace root links to it.
